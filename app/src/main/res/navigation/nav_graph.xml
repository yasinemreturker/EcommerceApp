<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.turker.ecommerceapp.presentation.fragment.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <!-- Profile Fragment -->
    <fragment
        android:id="@+id/cartFragment"
        android:name="com.turker.ecommerceapp.presentation.fragment.CartFragment"
        android:label="Cart"
        tools:layout="@layout/fragment_cart" />

    <!-- Settings Fragment -->
    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.turker.ecommerceapp.presentation.fragment.FavoriteFragment"
        android:label="Favorite"
        tools:layout="@layout/fragment_favorite" />

    <!-- Notifications Fragment -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.turker.ecommerceapp.presentation.fragment.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />
</navigation>